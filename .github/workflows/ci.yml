name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-format:
    name: 🔍 Lint & Format
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.21.1, 24.11.1]
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v6

      - name: 🔧 Setup Project
        uses: ./.github/actions/setup
        with:
          node-version: ${{ matrix.node-version }}

      - name: 🛠️ Setup Biome
        uses: biomejs/setup-biome@v2
        with:
          version: latest

      - name: 🔍 Run Biome CI
        run: biome ci .

      - name: 🔍 Run lint
        run: npm run lint

  type-check:
    name: 🔧 Type Check
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.21.1, 24.11.1]
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v5

      - name: 🔧 Setup Project
        uses: ./.github/actions/setup
        with:
          node-version: ${{ matrix.node-version }}

      - name: 🍱 Run TypeScript compiler
        run: npx tsc --noEmit

  test:
    name: 🧪 Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.21.1, 24.11.1]

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v5

      - name: 🔧 Setup Project
        uses: ./.github/actions/setup
        with:
          node-version: ${{ matrix.node-version }}

      - name: 🧪 Run tests
        run: npm test

  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.21.1, 24.11.1]
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v5

      - name: 🔧 Setup Project
        uses: ./.github/actions/setup
        with:
          node-version: ${{ matrix.node-version }}

      - name: 🏗️ Build package
        run: npm run build
